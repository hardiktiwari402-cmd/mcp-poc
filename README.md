1) Backend
cd backend
cp .env.example .env   # fill keys
npm install
node server.js

2) Frontend
cd frontend
npm install
npm run dev

Open frontend (vite default) and enter owner/repo then click Generate.
